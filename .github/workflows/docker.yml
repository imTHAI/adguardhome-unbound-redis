name: Build and Push Multi-Arch Docker Image

on:
  # 1. Manual trigger (requested)
  workflow_dispatch: 
    inputs:
      tag:
        description: 'Tag/Version to build (e.g., v0.107.70)'
        required: true
        default: 'latest'

  # 2. External API trigger (via repository_dispatch)
  repository_dispatch:
    types: [new_adguardhome_release] # Event name sent by the GitHub monitoring script

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Setup for multi-arch build
      - name: Set up QEMU (for ARM64 emulation)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Login to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # Login to GitHub Container Registry
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      # Build and push the multi-arch image
      - name: Build and push multi-arch image
        uses: docker/build-push-action@v5
        
        # Define the tag value
        env:
          TAG_VALUE: ${{ github.event.client_payload.tag_name || github.event.inputs.tag }}
        
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          
          # --- CORRECTION ---
          # 1. Add build-args
          build-args: |
            AGH_VERSION=${{ env.TAG_VALUE }}
            
          # --- CORRECTION ---
          # 2. To use env.TAG_VALUE in the tags
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/adguardhome-unbound-redis:${{ env.TAG_VALUE }}
            ghcr.io/imthai/adguardhome-unbound-redis:${{ env.TAG_VALUE }}
            ${{ secrets.DOCKERHUB_USERNAME }}/adguardhome-unbound-redis:latest
            ghcr.io/imthai/adguardhome-unbound-redis:latest
